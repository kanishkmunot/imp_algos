#include <iostream>
#include<vector>
using namespace std;

int main(){

    bool check(){
        int n = weights.size();
        
    }

    int shipWithinDays(vector<int>& weights, int days) {
        int low = max(weights);
        int sum = 0;
        for(int i=0; i<n; i++){
            sum+=weights[i];
        }
        int high = 
        cout<<low;
    }

}

